services:
  app-hub:
    build: .
    container_name: app-hub
    ports:
      - "3002:3002"
    volumes:
      - app-data:/app/server/data
      - app-uploads:/app/server/uploads
    restart: unless-stopped

volumes:
  app-data:
  app-uploads:
